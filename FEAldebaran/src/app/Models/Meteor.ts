export class Meteor{
    
 
    constructor(public id: string, public des: string, public range: string, public ps_max: string, public fullname: string, public v_inf: string, public h: string, public last_obs_jd: string, public ip: string, public ps_cum: string, public last_obs: Date, public n_imp: number, public diameter: string, public ts_max: string, public author: string){}
}